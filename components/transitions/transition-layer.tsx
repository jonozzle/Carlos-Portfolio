// components/transitions/transition-layer.tsx
"use client";

export default function TransitionLayer() {
    // DELETE THIS FILE (or keep as a no-op) — do not append/remove #transition-layer manually.
    // The layer already exists in app/(main)/layout.tsx and should exist exactly once.
    return null;
}
